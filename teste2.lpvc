procedimento aniversario(numero dia, numero mes) : texto {
    numero primeiro = 1;
    texto segundo = 'segundo';
    texto terceiro = 'terceiro';

    retorne segundo + terceiro;
};

texto sla = aniversario(3, 2);

aniversario(1, 1);